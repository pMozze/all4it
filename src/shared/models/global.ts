export interface Global {
  page: {
    title: string;
    description: string;
  };
  logotype: string;
  contacts: {
    telegram: string;
    email: string;
  };
  footer: {
    title: string;
  };
}
